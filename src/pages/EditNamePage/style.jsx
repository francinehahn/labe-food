import styled from "styled-components"

export const EditNameStyle = styled.form `
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-top: 15px;
    p {
        margin: 5px 32px;
        letter-spacing: -0.29px;
        font-size: 12px;
        color: var(--red);
    }
`
