import styled from "styled-components"

export const EditNameStyle = styled.section `
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-top: 15px;
    form > p {
        margin: 0 32px;
        letter-spacing: -0.29px;
        font-size: 12px;
        color: var(--red);
    }
`
